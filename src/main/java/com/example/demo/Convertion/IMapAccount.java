package com.example.demo.Convertion;

import com.example.demo.EntityDto.AccountDto;
import com.example.demo.entity.Account;



public class IMapAccount implements MapStructMapperAccount {

	/*
	 * @Override public Account accountDtoToAccount(AccountDto accountDto) { Account
	 * account = new Account(); account.setCompte(accountDto.getCompte());
	 * account.setClient(accountDto.getCustomer());
	 * account.setCurrencyIsoCode(accountDto.getDevise());
	 * 
	 * 
	 * return null; }
	 */



}
